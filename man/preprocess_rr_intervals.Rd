% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{preprocess_rr_intervals}
\alias{preprocess_rr_intervals}
\title{Preprocess RR Intervals with Multiple Filtering Steps}
\usage{
preprocess_rr_intervals(
  rr_intervals,
  min_rr = 272,
  max_rr = 2000,
  window_size = 7,
  threshold = 0.2,
  centered_window = FALSE
)
}
\arguments{
\item{rr_intervals}{Numeric vector of RR intervals in milliseconds}

\item{min_rr}{Minimum physiologically plausible RR interval in milliseconds.
Default is 272 ms}

\item{max_rr}{Maximum physiologically plausible RR interval in milliseconds.
Default is 2000 ms}

\item{window_size}{Size of the moving window for artifact detection.
Default is 7 intervals}

\item{threshold}{Threshold for artifact detection as a proportion.
Default is 0.2}

\item{centered_window}{Logical indicating whether the moving window should
be centered. Default is FALSE}
}
\value{
A list containing:
\itemize{
\item \code{cleaned_rr}: Numeric vector of cleaned RR intervals (artifacts removed)
\item \code{valid_mask}: Logical vector indicating which original intervals are valid
\item \code{n_removed_physiological}: Number of intervals removed for physiological reasons
\item \code{n_removed_artifacts}: Number of intervals removed as artifacts
\item \code{n_original}: Original number of intervals
\item \code{n_final}: Final number of valid intervals
}
}
\description{
Applies a comprehensive preprocessing pipeline to RR intervals, including
physiological filtering and artifact detection. This is a convenience function
that combines multiple preprocessing steps.
}
\details{
The preprocessing pipeline applies the following steps in order:
\enumerate{
\item Physiological filtering using \code{\link[=filter_physiological_rr]{filter_physiological_rr()}}
\item Artifact detection using \code{\link[=detect_rr_artifacts]{detect_rr_artifacts()}}
\item Combines both filters to create final valid mask
\item Returns both cleaned data and diagnostic information
}

This function is designed to be the main entry point for RR interval
preprocessing in the HRV analysis pipeline.
}
\examples{
\dontrun{
rr_data <- c(800, 850, 200, 900, 2500, 820, 890, 870)
result <- preprocess_rr_intervals(rr_data)
print(paste("Removed", result$n_removed_physiological + result$n_removed_artifacts,
            "of", result$n_original, "intervals"))
clean_data <- result$cleaned_rr
}

}
